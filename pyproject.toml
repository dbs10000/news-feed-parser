[project]
name = "news-feed-parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dbs10000", email = "d.burnsidesmith@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "feedparser>=6.0.14",
]

[project.scripts]
news-feed-parser = "news_feed_parser:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
